{
  // Solution-File: enthaelt selbst keine Dateien, sondern verdrahtet nur die
  // drei Pakete. "pnpm typecheck" ruft "tsc -b" hierauf und baut shared bei
  // Bedarf selbst neu, bevor server und client geprueft werden.
  "files": [],
  "references": [
    { "path": "./packages/shared" },
    { "path": "./apps/server" },
    { "path": "./apps/client" }
  ]
}
